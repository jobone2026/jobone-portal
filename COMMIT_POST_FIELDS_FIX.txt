Fix: Save organization, total vacancies, and last date fields in admin post creation

- Updated PostController store() method to accept and save organization, total_posts, last_date, notification_date fields
- Updated PostController update() method to accept and save the same fields
- Added notification_date field to admin post form
- Changed form layout from 2-column to 3-column grid for better organization
- All fields now properly validated and saved to database
- Public pages already display these fields correctly

Fixes issue where admin could enter these values but they were being set to null on save.
