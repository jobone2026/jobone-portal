I cannot push to your GitHub repository (permission denied).

You have 2 options:

OPTION 1: Apply fix directly on server (FASTEST)
=================================================
Copy apply-fix-on-server.sh to your server and run:

    scp apply-fix-on-server.sh ubuntu@3.108.161.67:/var/www/jobone/
    ssh ubuntu@3.108.161.67
    cd /var/www/jobone
    bash apply-fix-on-server.sh

This will automatically fix both files and restart services.


OPTION 2: Push from your machine
=================================
If you have GitHub access configured on your local machine:

    cd govt-job-portal-new
    git push origin main

Then on server:
    cd /var/www/jobone
    git stash
    git pull origin main
    php artisan view:clear
    sudo systemctl restart php8.2-fpm


WHAT WAS FIXED:
===============
Changed delete buttons to pass 'slug' instead of numeric 'id' because:
- State and Category models use slug as route key
- Routes expect slug, not id
- Old: /admin/states/2 → 404 error
- New: /admin/states/arunachal-pradesh → works

Files changed:
- resources/views/admin/categories/index.blade.php (line 157, 195)
- resources/views/admin/states/index.blade.php (line 140, 174)
